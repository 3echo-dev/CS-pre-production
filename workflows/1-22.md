# Workflow: 1-22 (v0.1.0)

Kinds: `preproduction`.
`plan-job.js` keeps a row when its Condition holds. Conditions: `always`, `if:tag`, `unless:tag`.
Tags for this workflow: `has_trailer` (trailer milestones tracked separately), `multi_day` (more than one shoot day), `gate_A`, `gate_B`, `gate_C`.
Role is the agent's role name, or `-` for a script row or a board row (a gate the human decides on the 1-22 Control board). Owner is always `orchestrator` for this workflow; every director is `support`.
`XX` in the Task column means the row waits on a human and the board card is set to `needs_input`.

| # | Stage | Task | Condition | Agent | Role | Owner | Skills | Artifact | State after | Gate |
|---|---|---|---|---|---|---|---|---|---|---|
| 0 | Open | Pull the Drive folder (Brief, Concept, Client Assets) into `inputs/{client}/{job}/`; write job.json (script format, storyboard style, shoot days unknown); route; plan. Open the project on the board: `board-sync.js open` | always | orchestrator | orchestrator | owner | project-intake, drive-pull | job.json, route.json, plan.md | PLANNED | |
| 0a | Intake | Index every file; write the unified brief with the conflict list and the open questions. Questions go to the board inbox, never to chat alone | always | intake-director | intake-director | support | the-creative-director (wrapped), defuddle | brief.md | BRIEF_READY | |
| 1a | Scraper | XX. Brief-specific search across the client's site list only; every reference keeps URL, retrieval date, why it fits; unreachable sites recorded as gaps. the creative director selects on the board | always | reference-scout | reference-scout | support | inspiration-references, video-watch, defuddle | references/board.md | REFERENCES_READY | |
| 1b | Script | Draft in the chosen format only (screenplay or AV). One file per version; `record-version.js` after each. the creative director edits directly; the selected version is the one the approval hash binds | always | script-director | script-director | support | the-creative-director (wrapped), no-ai-slop | script/v{n}.md | SCRIPT_DRAFTED | |
| 1c | Storyboard | Panels for the selected script in ONE style. Panel id, story order and shoot order are three fields. One sample panel approved before the batch; images through 3echo only after the sample. The row is not done at the panel table: the orchestrator quotes and generates through `make-image` before 1d, and Gate A is never asked for on a table alone | always | storyboard-director | storyboard-director | support | generative-frame-craft, clip-director (wrapped), storyboard, make-image | storyboard/v{n}/panels.md, storyboard/v{n}/P*.png | STORYBOARD_DRAFTED | |
| 1d | Shot list | Rows with stable ids from script and board; grouped labels such as 7/8 preserved; every row traces to a scene and a panel. `shot-list-check.js` refuses an orphan row | always | shot-list-director | shot-list-director | support | clip-director (wrapped) | shot-list.csv | SHOT_LIST_DRAFTED | |
| 1e | Budget sheet | XX. the client's template pre-filled with what is known; unknown stays unknown, never zero; currency and rate source recorded | always | production-planner | production-planner | support | - | budget.xlsx | PLANNING_DRAFTED | |
| 1f | Timeline | XX. Milestone calendar from the HTF template: submission, feedback, approval, delivery as separate events; trailer tracked separately when routed | always | production-planner | production-planner | support | - | timeline.xlsx | PLANNING_DRAFTED | |
| 1h | Review pass A | Spawn a reviewer on `review-pass` against the Gate A section of `playbooks/review-rubrics.md`, with `shot-list-check.js` and `gate-a-check.js` output in hand; `gate-a-check.js` exits 1 while any panel has no image on disk unless a person recorded `imagesDeferred` in the manifest. NEEDS REVISION writes `revisions/{n}.json` and re-enters the row that owns the item; at most two rounds, then the gate opens with the warnings attached | always | orchestrator | orchestrator | owner | review-pass | validation/review-A-{n}.md | PLANNING_DRAFTED | |
| 1g | Gate A | Creative lock. `board-sync.js push` then end the turn. Next turn `board-sync.js pull` reads `gates/A`; `record-approval.js` binds the six items' hashes | always | board | - | - | review | approvals/A-{n}.json | AWAITING_GATE_A | A |
| 2a | Audio | VO, BGM and SFX as separate rows per scene, informed by the board; library source per row; missing VO is not applicable, not blank. Voice choice is a board question | always | audio-supervisor | audio-supervisor | support | openmontage-craft (wrapped), vo-roundtrip | audio.md | LOGISTICS_OPEN | |
| 2b | Talents | XX. Empty register with six fields per talent: name, picture, age, availability, cost, loading. Entered on the board; `board-sync.js pull` lands rows in `registers/talents.json`; cost and loading update the budget sheet, availability updates only the affected call sheets | always | logistics-registrar | logistics-registrar | support | - | registers/talents.json | LOGISTICS_OPEN | |
| 2c | Props | XX. Skeleton from script and board; key props declared by a human; source text preserved where a prop lives inside wardrobe or requests | always | logistics-registrar | logistics-registrar | support | - | registers/props.json | LOGISTICS_OPEN | |
| 2d | Locations | XX. Skeleton from script and board; real locations and availability declared by a human; fields match the HTF call sheet location block | always | logistics-registrar | logistics-registrar | support | - | registers/locations.json | LOGISTICS_OPEN | |
| 2f | Review pass B | Spawn a reviewer on `review-pass` against the Gate B section of the rubric, with `gate-b-check.js` output in hand. NEEDS REVISION re-enters the owning row; two rounds at most, then the gate opens with warnings | always | orchestrator | orchestrator | owner | review-pass | validation/review-B-{n}.md | LOGISTICS_OPEN | |
| 2e | Gate B | Logistics lock. Passes only when every register is approved or explicitly not applicable and audio is approved. `gate-b-check.js` exits 1 on any row with a blank that is not marked unknown-by-decision | always | board | - | - | review | approvals/B-{n}.json | AWAITING_GATE_B | B |
| 3a | Concept breakdown | Compile the MOT-format table from shot list, storyboard, audio and the three registers into the client's Excel template; column order preserved; the two same-label client-input columns kept separate; continued rows never duplicate a shot. `breakdown-check.js` compares a sample row to its source records | always | breakdown-compiler | breakdown-compiler | support | - | breakdown.xlsx, validation/breakdown-check.md | BREAKDOWN_DRAFTED | |
| 3b | Call sheets | One sheet per shoot day in the HTF layout from the rows assigned to that day; shooting order kept, not renumbered; overnight blocks computed; parallel units allowed with shared-resource conflicts flagged. `call-sheet-check.js` refuses a row whose talent or location is still unknown | always | call-sheet-builder | call-sheet-builder | support | - | call-sheets/day-{d}.xlsx | CALL_SHEETS_DRAFTED | |
| 3c | Review pass C | Spawn a reviewer on `review-pass` against the Gate C section of the rubric, with `breakdown-check.js` and `call-sheet-check.js` output in hand. NEEDS REVISION re-enters the owning row; two rounds at most, then the gate opens with warnings | always | orchestrator | orchestrator | owner | review-pass | validation/review-C-{n}.md | CALL_SHEETS_DRAFTED | |
| 3d | Gate C | Documents released. Production lead approves each day's sheet on the board; `check-approval.js` then `build-release.js` copies breakdown and every approved day sheet with the manifest of hashes | always | board | - | - | review | approvals/C-{n}.json, release/ | AWAITING_GATE_C | C |
| 3e | Shoot days to timeline | Write the issued shoot days back into the timeline and push the final board state | always | scripts | - | - | board-sync.js | timeline.xlsx | RELEASED | |

## Change propagation (R04)

A change request on the board for any approved item sets it and every dependent item to `needs_review`, reopens only the gate that item belongs to, and lands as an inbox row the orchestrator reads on `board-sync.js pull`. The orchestrator re-enters the row that owns the changed item; nothing downstream is regenerated until that row is re-approved. Previous versions are never deleted.

| Changed | Sent back for review |
|---|---|
| intake | scraper, script, storyboard, shot list, budget, timeline, audio, registers, breakdown, call sheets |
| script | storyboard, shot list, budget, timeline, audio, props, locations, breakdown, call sheets |
| storyboard | shot list, audio, props, locations, breakdown, call sheets |
| shot list | budget, timeline, talents, props, locations, breakdown, call sheets |
| any register or audio | breakdown, call sheets |
| timeline | call sheets |
