{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ProjectSpec v1 (1-22)",
  "type": "object",
  "required": [
    "schemaVersion",
    "jobId",
    "client",
    "title",
    "kind",
    "driveFolder",
    "scriptFormat",
    "storyboardStyle",
    "hasTrailer",
    "inputs",
    "approvers",
    "requestedAt"
  ],
  "properties": {
    "schemaVersion": {
      "const": "1.0"
    },
    "jobId": {
      "type": "string",
      "pattern": "^job-\\d{8}-[a-z0-9][a-z0-9-]*$"
    },
    "client": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "brand": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "enum": [
        "preproduction",
        "postproduction"
      ]
    },
    "driveFolder": {
      "type": "string",
      "minLength": 1,
      "description": "Where the inputs came from: a folder path on this computer or a Google Drive folder link, as drive-pull recorded it in manifest.source"
    },
    "scriptFormat": {
      "enum": [
        "screenplay",
        "av_script"
      ]
    },
    "storyboardStyle": {
      "enum": [
        "sketches",
        "live_pictures",
        "cartoon_animation"
      ]
    },
    "aspectRatio": {
      "description": "The frame every storyboard panel is drawn in; asked at intake, carried into the generation manifest, and the board's card ratio until the first panel is drawn",
      "enum": ["16:9", "9:16", "1:1", "4:3", "3:4", "21:9", "2:3", "3:2", null]
    },
    "hasTrailer": {
      "type": "boolean"
    },
    "shootDays": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0,
      "description": "Shoot days once the person answers; 0 means fully AI-generated with no live shoot; null until answered"
    },
    "inputs": {
      "type": "object",
      "required": [
        "brief",
        "concept",
        "assets"
      ],
      "properties": {
        "brief": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "concept": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "assets": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "approvers": {
      "type": "object",
      "required": [
        "creative",
        "logistics",
        "release"
      ],
      "properties": {
        "creative": {
          "type": "string",
          "minLength": 1
        },
        "logistics": {
          "type": "string",
          "minLength": 1
        },
        "release": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "requestedAt": {
      "type": "string",
      "minLength": 1
    },
    "notes": {
      "type": "string"
    }
  }
}
